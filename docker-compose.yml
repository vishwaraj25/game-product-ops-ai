services:
  postgres:
    image: postgres:16-alpine
    container_name: game-product-ops-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-game_ops}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-game_ops_password}
      POSTGRES_DB: ${POSTGRES_DB:-game_product_ops}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-game_ops} -d ${POSTGRES_DB:-game_product_ops}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
